# CLASS INITIALIZATION POC

Proof of concept for Typescript class initialization during instantiation

Idea is to initialize class in a similar fashion that C# does

e.g.

var user = new User { age: 20 };